import LoginForm from '@/chunks/LoginForm/LoginForm';

export default function LoginPage() {
  return (
    <main className="grid min-h-screen place-items-center">
      <LoginForm />
    </main>
  );
}
